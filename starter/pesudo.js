// import inquirer
// import filesystem 

// Create git ignore before installing npm dependencies 
// Create repro, Create gitignore (add files to ingore) // Ignore the template
// Install npm, then create dependencies > inquirer. 


// createREADMEfile() empty. 
    // inside the inquirer class, call the prompt method. 
    // Prompt user to insure name of file
    // Then, create a file with this name. (maybe do a check? Confirm the file name etc )


// writeREADfile()
    // Check - Does a read me file exist. If it doesnt, then go to createREADMEfile 
    // Prompt: Name / Type / Message for each: 
        // Description 
        // * Table of Contents 
        // * Installation 
        // * Usage 
        // * License (Add Choices)
        // * Contributing 
        // * Tests 
        // * Questions ()

        // * Questions ()
        // * Contributing 
        // * Table of Contents 




// prompt
    // Description 
    // Get info from user

// then 
    // data = writeREADfile(answers.description)
    // Store information from user, into README literal text, 
    // Data will return a string of infomation that can be passed into a file
    // Store this by calling the the function, and passing through the 


// CREATE TEMPLATE 
// 1. Create an empty read me file
// 2. Create the template for the read me
        // Which will be a direct literal, with assigned deconstructed object elements.

// GET INFORMATION 
// PROMPT
// 1. Ask user questions
// 2. Get the response from user
// WHEN > response has been recieved then 
// 3. Store the reponse in object


    // MODIFY INPUT to match critera 
    // For reponses that are links 
    // Create a new function, that takes the information and wraps it in a link 

    // For responses that are switch/case, get the assoicated information for the choice selected. 

// <THEN> WRITE FILE 
// 1. 


// Do we have to create repos from the command line or can we // Create repro, Create gitignore (add files to ingore), at this point? 
